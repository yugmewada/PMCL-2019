<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <uses-permission-sdk-23 android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission-sdk-23 android:name="android.permission.WRITE_EXTERNAL_STORAGE" />



    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".WelcomeActivity" android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity" android:theme="@style/AppTheme"></activity>
        <activity android:name=".ImageProcessActivity" android:theme="@style/AppTheme"></activity>
        <activity android:name=".CarRegisterActivity"  android:theme="@style/AppTheme"></activity>
        <activity android:name=".CarUpdateActivity" android:theme="@style/AppTheme"></activity>
        <activity android:name=".HomePageActivity"  android:theme="@style/AppTheme"></activity>
    </application>

</manifest>